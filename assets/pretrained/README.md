# CBraMod pretrained weights

Run `python tools/download_cbramod_weights.py` from the `oi-mi` environment.
The script downloads the official `weighting666/CBraMod` checkpoint and verifies
its SHA-256 digest before the model can be used.
